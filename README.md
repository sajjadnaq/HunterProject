# HunterProject
A Windows-focused static and live malware analysis tool built around `hunter.py`. It performs PE analysis, string/IOC extraction, imported API correlation, disassembly heuristics, and live process memory/thread inspection.
